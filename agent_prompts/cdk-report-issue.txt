You are authoring a CDK plugin skill definition for the ai-dev-toolkit repository.

## Your Task

Create the `cdk-report-issue` skill — collects bug/feature details and files a GitHub issue in ai-dev-toolkit.

## Output

Return ONLY the complete markdown file content. No commentary before or after.

**Target file path:** `src/plugins/cdk/skills/cdk-report-issue/SKILL.md`

## Required Frontmatter

```yaml
---
name: cdk-report-issue
description: Collects bug or feature request details and files a GitHub issue in NTTDATA-Launch/ai-dev-toolkit. Use when the user wants to report a bug, request a feature, or file an issue against the ai-dev-toolkit.
---
```

Include `{{{HEADER}}}` after frontmatter.

## Body Structure

### Purpose
Capture actionable bug/feature context. Create GitHub issue in `NTTDATA-Launch/ai-dev-toolkit` without leaving coding session.

### Target Repository
https://github.com/NTTDATA-Launch/ai-dev-toolkit

### 5-step Workflow

**1. Gather issue details interactively:**

Required for all types:
- Issue type: `bug`, `feature request`, `docs issue`, `workflow issue`
- Short title (5-12 words)
- Problem statement (1-2 sentences)
- Impact/severity: `low`, `medium`, `high`

Required by type:
- bug/workflow issue: expected behavior, actual behavior, reproduction steps (numbered, concise)
- feature request: desired outcome, why current behavior insufficient
- docs issue: doc/page location, what is unclear/incorrect

Optional: error output, screenshot/log link, related feedback URL, suggested fix

**2. Add triage context automatically:**
- Current branch (`git branch --show-current`)
- Modified files (`git status --short`)
- Current OS if available
- Skill used if known
- Toolkit/plugin version if discoverable

**3. Draft final issue body:**
- Title: `[pilot][<type>] <short title>`
- Clear markdown sections
- Reproduction max ~5 steps, deterministic
- Separate Required Context from Optional Context
- Suggested labels as plain text if unavailable

**Issue body template** (document in skill):
```markdown
## Problem Statement
{text}

## Type / Severity
{type} / {severity}

## Required Context
### Expected Behavior (bug/workflow)
### Actual Behavior (bug/workflow)
### Reproduction Steps (bug/workflow)
1.

## Optional Context
- Branch:
- Modified files:
- OS:
- Skill used:
- Error output:

## Suggested Labels
pilot, {type}
```

**4. Confirm submission** — show title, type, severity, summary; ask explicit approval

**5. Create GitHub issue:**
- Preferred: `gh issue create --repo NTTDATA-Launch/ai-dev-toolkit --title "..." --body-file <temp-file>`
- Build body in `.tmp/issue-body.md` first (create parent if needed)
- Avoid interactive heredocs
- Clean up after success
- Capture and return issue URL; retry once if missing
- Fallback: copy/paste markdown + https://github.com/NTTDATA-Launch/ai-dev-toolkit/issues/new/choose

### CHECKPOINT
Report: repository, title, type/severity, URL.
Suggest `/cdk:provide-feedback` if from pilot experience.

## Supporting Files

### Runtime temp file: `.tmp/issue-body.md`
Created in step 5, cleaned up after successful submission.

**No committed source supporting files.**

## Integration

- **Type:** Utility skill
- **Invoked via:** `/cdk:report-issue`
- **Related:** `/cdk:provide-feedback`

## Quality Bar

- Type-specific required fields clearly separated
- Auto triage context from git/session
- Title convention `[pilot][<type>]`
- Body-file pattern (no shell piping of multiline markdown)
- Fallback manual link
