You are authoring a CDK plugin agent definition for the ai-dev-toolkit repository.

## Your Task

Create a markdown agent file that defines the `cdk-code-reviewer` agent — an expert code reviewer for guidelines compliance, bug detection, and code quality. The output must closely match the existing CDK agent conventions.

## Output

Return ONLY the complete markdown file content. No commentary before or after.

**Target file path:** `src/plugins/cdk/agents/code-reviewer.md`

## Required Frontmatter

```yaml
---
name: cdk-code-reviewer
description: Review local changes for guidelines compliance, bug detection, and code quality

claude:
  allowed-tools:
    - Read
    - Grep
    - Glob
    - Bash
  model: inherit

copilot:
  user-invocable: false
  model: Auto (copilot)
---
```

Include `{{{HEADER}}}` immediately after frontmatter.

## Body Structure

### Role
Expert code reviewer across languages/frameworks. Review against project guidelines with high precision to minimize false positives.

### Review Scope (critical section)
Invoked by `cdk-review` orchestrator which determines scope. Task prompt specifies:
- Scope name: `local`, `branch`, or `full`
- Exact file list to review
- Exact diff command (e.g., `git diff HEAD -- <file>` or `git diff <base>...HEAD -- <file>`)

**Only review files/changes specified by orchestrator.** Do NOT expand scope with arbitrary git diff commands.

### Core Review Responsibilities
Three areas:
1. **Project Guidelines Compliance** — CLAUDE.md or equivalent rules: imports, framework conventions, style, error handling, logging, testing, platform compatibility, naming
2. **Bug Detection** — logic errors, null handling, race conditions, memory leaks, security, performance
3. **Code Quality** — duplication, missing error handling, accessibility, inadequate test coverage

### Issue Confidence Scoring
Rate 0–100 with bands:
- 0–25: likely false positive or pre-existing
- 26–50: minor nitpick not in guidelines
- 51–75: valid but low-impact
- 76–90: important
- 91–100: critical bug or explicit violation

**Only report issues with confidence >= 80**

### Output Format
- Start by listing what is being reviewed
- For each high-confidence issue: description, confidence score, file:line, guideline/bug explanation, concrete fix
- Group by severity:
  - **CRITICAL** (90–100): must fix before PR
  - **WARNING** (80–89): should fix
- If no high-confidence issues: confirm code meets standards with brief summary
- Emphasize quality over quantity; filter aggressively

## Supporting Files & Integration

Spawned by `cdk-review` skill with skip key `code`.

**Orchestrator passes:** scope name, file list, diff command per file.

**No supporting files** — advisory/review-only agent.

## Quality Bar

- Scope restriction is repeated and unambiguous (prevents scope creep)
- Confidence threshold (>= 80) is explicit
- Severity grouping matches confidence bands
- Read-only review agent (no file modification instructions)
