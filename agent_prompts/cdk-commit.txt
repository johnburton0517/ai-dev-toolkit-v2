You are authoring a CDK plugin skill definition for the ai-dev-toolkit repository.

## Your Task

Create the `cdk-commit` skill — a utility skill that stages and creates conventional git commits with optional GitHub issue references.

## Output

Return ONLY the complete markdown file content. No commentary before or after.

**Target file path:** `src/plugins/cdk/skills/cdk-commit/SKILL.md`

## Repository Conventions

- `{{{HEADER}}}` immediately after frontmatter
- Skills use nested directory layout: `skills/<skill-name>/SKILL.md`
- No `claude:`/`copilot:` frontmatter needed unless ecosystem-specific behavior differs
- `<CHECKPOINT>...</CHECKPOINT>` blocks define user-facing completion messages

## Required Frontmatter

```yaml
---
name: cdk-commit
description: Stages and creates a conventional commit (feat, fix, docs, style, refactor, test, chore) with optional GitHub issue reference. Use when the user asks to commit, create a git commit, or save their changes.
---
```

Description must include trigger phrases ("Use when the user asks to commit...").

## Body Structure

### Commit Format section
Show format:
```
type: short description [TICKET-ID optional]

Optional longer description (wrap ~72 chars).
```

**Types table** with columns: Type, When to use, Example
Include: feat, fix, docs, style, refactor, test, chore, perf, ci, build

Each example uses `[#123]` ticket format.

Note: Omit `[#123]` block entirely when no ticket applies.

**Quick checks:**
- Ticket? Append `[TICKET-ID]` or omit brackets entirely
- Breaking? Add `!` after type (e.g., `feat!: ...`) and call out in body
- Imperative, concise summary; no trailing punctuation
- Subject ≤50 chars; body wrap ~72 chars
- AI-assisted: append Co-Authored-By after body

### Workflow section
Pre-run git context commands (Copilot-style inline execution):
```
- Current git status: !`git status`
- Current git diff (staged and unstaged changes): !`git diff HEAD`
- Current branch: !`git branch --show-current`
- Recent commits: !`git log --oneline -10`
```

Instructions:
- Based on changes, create a single git commit
- Call multiple tools in single response
- Stage and commit in one message
- Do not use other tools or send other text besides tool calls

### CHECKPOINT block
```
<CHECKPOINT>
Inform user:

> Commit created: `<hash>`
> Message: `<message>`
>
> </CHECKPOINT>
```

## Supporting Files

**No additional source files** — this skill is self-contained in SKILL.md.

**Runtime artifacts:** None persisted to `.cdk/`. Operates directly on git state.

**Related plugin files referenced indirectly:**
- `.cdk/workflow-state.json` — may supply active ticket ID for commit message
- Conventional commit rules align with repo `CLAUDE.md` and git hooks (commitlint)

## Integration

- **Type:** Utility skill — runs anytime, no workflow stage checks
- **Invoked via:** `/cdk:commit`
- **Typical position:** After `/cdk:implement`, before or after `/cdk:review`, before `/cdk:create-pr`

## Quality Bar

- Complete commit type table with examples
- Git context gathering before commit
- Strict "only tool calls, no other text" execution contract
- CHECKPOINT with hash and message placeholders
