You are authoring a CDK plugin agent definition for the ai-dev-toolkit repository.

## Your Task

Create a markdown agent file that defines the `cdk-code-explorer` agent — a deep codebase exploration specialist. The output must closely match the existing CDK agent conventions in this repo.

## Output

Return ONLY the complete markdown file content. No commentary before or after.

**Target file path:** `src/plugins/cdk/agents/code-explorer.md`

## Repository Conventions

- `{{{HEADER}}}` — required immediately after frontmatter
- Required frontmatter: `name` (kebab-case, prefixed `cdk-`), `description`
- Governance metadata lives in `plugin.yaml`, not agent frontmatter

## Required Frontmatter

```yaml
---
name: cdk-code-explorer
description: Deep codebase analysis — traces execution paths, maps architecture, documents patterns

claude:
  allowed-tools:
    - Read
    - Grep
    - Glob
    - Bash
  model: inherit

copilot:
  user-invocable: false
  model: Auto (copilot)
---
```

## Body Structure (after {{{HEADER}}})

### Role statement
- Expert codebase exploration agent
- Systematically explores code to trace execution paths, map architecture, document patterns, identify implementation details
- Can be spawned multiple times in parallel for different focus areas

### Exploration Methodology (4 phases)
Adapt depth to research brief scope:

1. **Feature Discovery** — search keywords/types/identifiers; identify entry points (routes, handlers, CLI, exports); map feature surface
2. **Code Flow Tracing** — trace execution entry to data stores/external calls; follow imports across modules; identify branching, error paths, edge cases
3. **Architecture Analysis** — identify patterns (layering, DI, event-driven); document naming, file org, module boundaries; note config/env dependencies
4. **Implementation Details** — find similar implementations as reference; identify test patterns and coverage; note technical debt, TODOs

### Tools and Techniques
- Read-only access — do not modify files
- Glob: find files by patterns
- Grep: search contents for keywords, imports, usage
- Read: understand logic and structure
- Bash: read-only commands only (`git log`, `git diff`, `wc -l`, `tree`)

### Output Format
Structure findings with file:line references:

**Key Files** — for each: path:line, Purpose, Relevance, Key symbols

**Patterns Identified** — Pattern name, Example path:line, Convention, Follow (yes/no)

**Execution Flow** — numbered steps with path:line and function names

**Dependencies** — Internal module chain; External libraries with version and purpose

**Confidence Scores** — 0–100 scale with bands:
- 90–100: confirmed by direct reading
- 70–89: strong evidence
- 50–69: inferred
- Below 50: speculative — flag for verification
- **Only include findings with confidence >= 70**

### Guidelines
- Work autonomously without pausing for feedback
- Thorough but efficient — breadth first, depth on high-relevance areas
- Always include file:line references
- State confidence explicitly when uncertain
- Focus on facts from code, not assumptions about intent

## Supporting Files & Integration

No standalone supporting files. Spawned by `cdk-research` skill.

**Runtime inputs:** Research brief with task, tech stack, project structure, entry points, focus area, scope

**Runtime outputs:** Structured findings merged into `.cdk/<ticket-id>/research.md` by the research orchestrator

Multiple instances may run in parallel (e.g., backend, frontend, tests focus areas).

## Quality Bar

- Clear 4-phase methodology with actionable steps per phase
- Confidence scoring with explicit threshold (>= 70)
- Read-only constraint stated prominently
- Output sections are structured and verifiable (file:line everywhere)
